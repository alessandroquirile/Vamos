<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quiriletelese.troppadvisorproject">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".views.IntroActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".views.AccomodationDetailMapsActivity"
            android:label="@string/title_activity_accomodation_detail_maps" />
        <activity
            android:name=".views.AttractionMapActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".views.RestaurantMapActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".views.AttractionsListActivity"
            android:label="@string/attractions" />
        <activity
            android:name=".views.RestaurantsListActivity"
            android:label="@string/restaurant" />
        <activity
            android:name=".views.HotelsListActivity"
            android:label="@string/hotel" />
        <activity android:name=".views.AttractionDetailActivity" />
        <activity
            android:name=".views.SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.RestaurantDetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.SeeReviewsActivity"
            android:label="@string/user_reviews"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.HotelDetailActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.WriteReviewActivity"
            android:label="@string/add_review"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.HotelMapActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".views.SignUpActivity"
            android:theme="@style/AppThemeLogin" />
        <activity
            android:name=".views.LoginActivity"
            android:theme="@style/AppThemeLogin" />
        <activity
            android:name=".views.HomePageActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan" />
    </application>

</manifest>